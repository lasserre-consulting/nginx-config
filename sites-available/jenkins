server {
    listen 80;
    server_name jenkins.lasserre-consulting.fr;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name jenkins.lasserre-consulting.fr;

    ssl_certificate /etc/letsencrypt/live/jenkins.lasserre-consulting.fr/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/jenkins.lasserre-consulting.fr/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    location / {
        proxy_pass http://localhost:8090;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Websockets (logs en temps réel)
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        proxy_read_timeout 90s;
    }
}
